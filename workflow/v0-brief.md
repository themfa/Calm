# Component draft brief for v0
Create only the requested component for a Next.js App Router application using React, TypeScript, and Tailwind.

Component: [name and purpose]
Reference: [attach the approved image]
Acceptance: [specific behavior, responsive layout, and states]

Read the attached spec.md excerpt. This is a personal portfolio page; Calm brand values are only a scoped specimen.
Use the supplied existing CSS variables for typography, surfaces, text, borders, spacing, and focus. Request missing token information instead of inventing a replacement theme.
Keep content and evidence factual. Include keyboard behavior and reduced-motion handling where relevant.
Return the component, imports, necessary dependencies, and any assumptions. Keep data mocked and identified until a real integration is assigned.

Handoff: Paul reviews the draft; Cursor integrates selected files into the feature branch and runs the project checks. Do not replace the full app or its configuration with a generated scaffold.
