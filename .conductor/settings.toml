"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"
[scripts]
setup = "npm ci"
run_mode = "concurrent"
[scripts.run.dev]
command = "npm run dev -- --port ${CONDUCTOR_PORT:-3100}"
default = true
icon = "play"
